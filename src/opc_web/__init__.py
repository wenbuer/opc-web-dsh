# -*- coding: utf-8 -*-
"""opc_web —— OPC 控制台（opc-web）后端包。

职责边界（R1 维护 · R0 拍板不变）：
  1. 只读知识库全部 .md（文件树/单文件/待决清单/每日简报）
  2. 唯一写操作：R0 在页面提交批阅 → 写入《决策/批阅台.md》对应待决的「R0 批阅」栏
  3. 不修改决策日志、不派发角色（那是 R1 的日常流程，仍按知识库纪律执行）

启动：python run.py 或 python -m opc_web → http://127.0.0.1:8901
目录约定 v1.10：单根目录（配置 root / env OPC_KB_ROOT 覆盖）→ 自动生成 批阅台/、工作区/（按角色名称）、知识库/；不再使用 决策/运营/营销 等静态分类。
"""
__version__ = "1.7.0"
